# Sun Tzu, Today is an EXTRACTED show: config + a generator + a public-domain text
# corpus, all text. Override the repo-root `marketing/** filter=lfs` catch-all so
# the source is stored inline (diffable), not as Git LFS pointers. Comments on own line.
*.js            text diff merge -filter
*.py            text diff merge -filter
*.md            text diff merge -filter
*.json          text diff merge -filter
*.txt           text diff merge -filter
*.env           text diff merge -filter
.gitattributes  text diff merge -filter
.gitkeep        text diff merge -filter
# Binary brand assets (if added later) stay in LFS:
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
